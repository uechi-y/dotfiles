#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

export HISTCONTROL=ignoreboth
export HISTSIZE=10000
export HISTFILESIZE=20000
export HISTTIMEFORMAT="%F %T "
export HISTIGNORE="ls"
shopt -s histappend

alias ls='ls --color=auto'
alias grep='grep --color=auto'

# https://starship.rs/
if [ $(command -v starship) ]; then
	eval "$(starship init bash)"
fi

# https://mise.jdx.dev
if [ $(command -v mise) ]; then
	eval "$(mise activate bash)"
fi

# https://github.com/HalFrgrd/flyline
if [[ -L ~/.local/lib/libflyline.so && -e ~/.local/lib/libflyline.so ]]; then
	enable -f ~/.local/lib/libflyline.so flyline
	flyline key remap Ctrl+F Right
	flyline key remap Ctrl+B Left
	flyline key remap Ctrl+P Up
	flyline key remap Ctrl+N Down
fi
