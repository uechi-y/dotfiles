#
# ~/.bash_profile
#

case ":$PATH:" in
  *":$HOME/.local/bin:"*) ;;
  *) export PATH="HOME/.local/bin:$PATH" ;;
esac

export EDITOR=hx
export SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket

[[ -f ~/.bashrc ]] && . ~/.bashrc
